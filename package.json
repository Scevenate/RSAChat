{
  "name": "rsachat",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "rm -r dist && bun build index.html --outdir dist/RSAChat",
    "dev": "bun run index.html",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@noble/ciphers": "^2.0.1",
    "@panva/hpke-noble": "^1.1.0",
    "hpke": "^1.1.0",
    "wrangler": "^4.83.0"
  }
}
