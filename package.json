{
  "name": "rsachat",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build": "rm -r dist && bun build index.html --outdir dist",
    "dev": "bun run index.html"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@noble/ciphers": "^2.0.1",
    "@panva/hpke-noble": "^1.1.0",
    "hpke": "^1.1.0"
  }
}
